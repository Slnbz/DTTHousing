<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DTTHousing" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->

        <item name="colorOnPrimary">@color/light_gray</item>
        <!-- Secondary brand color. -->

        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColorHint">@android:color/black</item>
    </style>
    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Gets rid of the search icon -->
        <item name="searchIcon">@drawable/ic_search_xml</item>
        <item name="android:tint">@color/black</item>
        <!-- Gets rid of the "underline" in the text -->
        <item name="queryBackground">@color/black</item>
        <!-- Gets rid of the search icon when the SearchView is expanded -->
        <item name="android:textColorHint">@color/light</item>
        <item name="android:fontFamily">@font/gothamssm_light</item>
        <item name="android:textAppearance">@style/SubtitleStyle</item>
    </style>
    <style name="myActionbarTransparent" parent="Theme.AppCompat">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:actionBarStyle">@style/ThemeActionBar</item>
        <item name="android:windowActionBarOverlay">true</item>
    <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/ThemeActionBar</item>
        <item name="windowActionBarOverlay">true</item>
    </style>
</resources>